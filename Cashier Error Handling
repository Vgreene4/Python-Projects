def interpretCashier(inputs):
    try:
        int(inputs)
        return "PIN"
    except:
        try:
            float(inputs)
            return "Transaction"
        except:
            return "Password"
#takes a string input and checks to see if it can be converted to int or float 
