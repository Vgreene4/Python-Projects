def check_balance(brackets):
    rb = 0
    lb = 0
    for bracket in brackets:
        if bracket == '[':
            rb += 1

        elif bracket == ']':
            lb += 1
        
    if lb != rb:
        return False 
    return True
