def string_match(a, b):
  count=0
  for i in range(0,len(a)):
      if a[i:i+2] == b[i:i+2]:
        count+=1
  if len(a)==len(b):
    count-=1
  return count
  
  #returns the number of times two strings have two characters in a row in common
